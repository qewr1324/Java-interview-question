# 📘 فاز ۱۱: الگوهای طراحی (Design Patterns) – مختصر، مفید و کاربردی

هدف این نیست که همه ۲۳ تا الگوی Gang of Four (GoF) رو حفظ کنی. فقط ۶-۷ تا از پرکاربردترین‌هاش رو که در Spring و مصاحبه‌ها میاد رو خوب بلد باش.

---

## ۱. Singleton (تک‌نمونه)

- **هدف:** اطمینان از اینکه یک کلاس فقط و فقط یک نمونه (instance) داشته باشد و یک نقطه دسترسی سراسری به آن.
- **کاربرد در Spring:** تمام Beanهای Spring به طور پیش‌فرض Singleton هستن. یعنی Spring Container فقط یک شیء از اون‌ها میسازه و همه جا همون رو تزریق میکنه.
- **سوال: چرا Singleton؟**
- **جواب:** مدیریت منابع مشترک (مثل کانکشن دیتابیس)، پیکربندی برنامه، یا سرویس‌های Stateless.

---

## ۲. Factory Method و Abstract Factory (روش کارخانه‌ای)

- **هدف:** واگذار کردن مسئولیت ساخت اشیاء به یک متد یا کلاس دیگر (Factory)، به جای استفاده از `new` در همه جای کد.
- **کاربرد در Spring:** کل `ApplicationContext` و `BeanFactory`، خودش یه Abstract Factory عظیمه که اشیاء (Beanها) رو خلق و مدیریت میکنه.

---

## ۳. Dependency Injection (تزریق وابستگی)

- **هدف:** برداشتن مسئولیت ایجاد وابستگی‌ها (Dependencies) از روی دوش کلاس و دادن اون به یک چارچوب خارجی (مثل Spring Container). این یعنی Inversion of Control (IoC).
- **چرا؟** کاهش coupling (وابستگی) و راحت‌تر شدن تست‌نویسی. می‌تونیم به راحتی Mock تزریق کنیم.
- **سوال طلایی: DI چطور پیاده‌سازی میشه؟**
  - **Constructor Injection:** (توصیه شده). وابستگی‌ها از طریق سازنده میان. شیء Immutable و کامل ساخته میشه.
  - **Field Injection:** با `@Autowired` روی فیلد. ساده‌ست ولی تست‌نویسی رو سخت می‌کنه.

---

## ۴. Proxy (نماینده)

- **هدف:** کنترل دسترسی به یک شیء. یک شیء Proxy مثل نماینده عمل میکنه و می‌تونه قبل یا بعد از صدا زدن متدهای شیء اصلی، کارهای اضافه انجام بده.
- **کاربرد در Spring:** یکی از مهم‌ترین‌ها. تراکنش‌های `@Transactional` با Proxy پیاده‌سازی میشن. Spring یک Proxy دور Bean می‌پیچه که کدهای باز و بسته کردن Transaction رو قبل و بعد از متد اصلی اجرا می‌کنه. (این که میگن چرا `@Transactional` روی متدهای `private` کار نمی‌کنه هم به خاطر محدودیت Proxy هست).

---

## ۵. Observer (مشاهده‌گر)

- **هدف:** تعریف یک وابستگی یک‌به‌چند بین اشیاء. وقتی وضعیت یک شیء (Subject) تغییر کنه، همه وابسته‌هاش (Observers) مطلع میشن.
- **کاربرد در Spring:** مکانیزم Event Handling در Spring. با `ApplicationEventPublisher` یه رویداد منتشر می‌کنی و با `@EventListener` بهش گوش میدی.

---

## ۶. Template Method (الگوی متد الگو)

- **هدف:** اسکلت یک الگوریتم رو در یک متد تعریف می‌کنه و بعضی قدم‌هاش رو به زیرکلاس‌ها واگذار می‌کنه.
- **کاربرد در Spring:** کلاس `JdbcTemplate` یا `RestTemplate`. این‌ها کارهای تکراری مثل باز و بسته کردن Connection رو انجام میدن و فقط کار اصلی (مثل نوشتن SQL) رو به گردن ما میندازن.

---

## ۷. Builder (سازنده)

- **هدف:** ساخت گام‌به‌گام یک شیء پیچیده. جدا کردن فرآیند ساخت از نمایش نهایی.
- **کاربرد:** وقتی یه کلاس کلی فیلد داره و نمی‌خوایم Constructor با کلی پارامتر داشته باشیم. در جاوا از Lombok با `@Builder` خیلی راحت پیاده میشه.

---

### ❓ سوال احتمالی مصاحبه: "با دیزاین پترن‌ها آشنایی داری؟ چندتاش رو نام ببر و بگو کجا ازشون استفاده کردی."

**حالا جوابت اینه:**
"بله. من توی کار با Spring به طور غیرمستقیم با خیلی‌هاشون درگیر بودم. برای مثال، Beanهای Spring همشون Singleton هستن. کل IoC Container بر اساس Factory و Dependency Injection کار می‌کنه. `@Transactional` با Proxy پیاده‌سازی شده. و خودم هم برای ساخت اشیاء پیچیده از Builder (مثلاً با Lombok) استفاده کردم."
