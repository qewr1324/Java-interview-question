# 📘 فاز ۲: برنامه‌نویسی شی‌گرا (Object-Oriented Programming) – کامل و عمیق

برنامه‌نویسی شی‌گرا فقط یاد گرفتن ۴ تا کلمه نیست (کپسوله‌سازی، وراثت، چندریختی، انتزاع). باید بفهمی این مفاهیم چه مشکلی رو حل می‌کنن، چطور توی کد واقعی پیاده میشن، و چرا اینقدر مهم هستن.

## 2.0 چرا اصلاً شی‌گرایی؟ مشکل برنامه‌نویسیِ رویه‌ای (Procedural) چی بود؟

تصور کن یه برنامه بانکی داری. هزاران تابع داری: `withdraw()`, `deposit()`, `calculateInterest()`... همه این توابع روی داده‌های سراسری (Global Variables) کار می‌کنن.

- **مشکل ۱:** هر تابعی می‌تونه داده رو خراب کنه. هیچ محافظی نیست.
- **مشکل ۲:** اگه ساختار داده عوض بشه (مثلاً حساب پس‌انداز بشه دو نوع: کوتاه‌مدت و بلندمدت)، باید تمام توابع رو عوض کنی. این میشه یه فاجعه.
- **مشکل ۳:** کد برای تیم‌های بزرگ و پروژه‌های عظیم غیرقابل مدیریت میشه.

شی‌گرایی این راه حل رو میده: داده و توابعی که روی اون داده کار می‌کنن رو بذار توی یه بسته به اسم شیء (Object) و بگو هیچکی حق نداره مستقیماً به داده دست بزنه. فقط از طریق اون توابع می‌تونی.

## 2.1 کلاس و شیء (Class & Object)

- **کلاس:** یه قالب یا نقشه هست. هیچ حافظه‌ای اشغال نمی‌کنه. (مثل نقشه ساختمان یا فرم خالی ثبت‌نام).
- **شیء:** یه نمونه واقعی از روی اون قالب که حافظه اشغال می‌کنه. (مثل ساختمونی که ساخته شده یا فرم پُر شده).

### ❗️ سوال مصاحبه‌ای ۱: توی حافظه، وقتی یه شیء می‌سازی دقیقاً چه اتفاقی می‌افته؟

**جواب:**

1. وقتی JVM به دستور `new` میرسه، میره توی Heap فضای کافی برای فیلدهای اون کلاس (و کلاس‌های والدی) کنار میذاره.
2. اون فضا رو با مقادیر پیش‌فرض (0, false, null) پر می‌کنه.
3. Constructor (سازنده) کلاس رو صدا میزنه تا فیلدها رو با مقادیر دلخواه ما مقداردهی اولیه کنه.
4. در نهایت، آدرس این شیء توی Heap، به یه متغیر Reference در Stack داده میشه.

```java
// a. اینجا فقط یه اشاره‌گر توی Stack ساخته میشه. هنوز هیچ شیئی وجود نداره.
BankAccount myAccount;

// b. اینجا اتفاقای بالا میفته و آدرس شیء جدید توی myAccount ذخیره میشه.
myAccount = new BankAccount();
```

## 2.2 سازنده‌ها (Constructors)

**سوال: Constructor چیه و فرقش با متد معمولی چیه؟**

**جواب:**

- هیچ نوع بازگشتی‌ای نداره (حتی `void`).
- همنام کلاسه.
- برای مقداردهی اولیه به فیلدهای شیء هنگام ساختش استفاده میشه.
- اگه هیچ سازنده‌ای نسازی، جاوا یه سازنده پیش‌فرض (Default Constructor) بدون پارامتر برات میسازه. ولی به محض این که خودت یه سازنده (حتی با پارامتر) بسازی، اون پیش‌فرض دیگه ساخته نمیشه.

```java
public class Person {
    private String name;

    // سازنده (Constructor)
    public Person(String name) {
        this.name = name; // this.name به فیلد کلاس اشاره میکنه، name به پارامتر
    }
}
// حالا این غلطه: Person p = new Person();
// این درسته: Person p = new Person("Ali");
```

### کلمه کلیدی this: همیشه به شیء فعلی (همینی که باهاش کار می‌کنیم) اشاره داره. برای فرق گذاشتن بین فیلد کلاس و پارامتر ورودی استفاده میشه

## 2.3 اصل اول: کپسوله‌سازی (Encapsulation)

**تعریف ساده:** بسته‌بندی داده‌ها (متغیرها) و متدهایی که روی آن‌ها کار می‌کنند، در یک واحد (کلاس)، و مخفی کردن جزئیات پیاده‌سازی از دنیای بیرون.

**چرا مهمه؟**

- **امنیت:** نمی‌ذاری بقیه مستقیم بیان و فیلدهای کلاس رو خراب کنن.
- **انعطاف‌پذیری:** هر وقت خواستی می‌تونی منطق داخلی رو عوض کنی. تا وقتی امضای متدهای `public` عوض نشه، هیچ کد دیگه‌ای در برنامه نمی‌شکنه.

**چطور پیاده‌سازی میشه؟**
با Access Modifiers (سطوح دسترسی):

| Modifier | داخل کلاس | داخل Package | زیرکلاس (ارث‌بری) | همه جا |
| --- | --- | --- | --- | --- |
| `private` | ✅ | ❌ | ❌ | ❌ |
| `default` (بدون کلمه) | ✅ | ✅ | ❌ | ❌ |
| `protected` | ✅ | ✅ | ✅ | ❌ |
| `public` | ✅ | ✅ | ✅ | ✅ |

### قانون طلایی برنامه‌نویسان حرفه‌ای

- فیلدها رو همیشه `private` کن.
- دسترسی بهشون رو فقط از طریق متدهای `public` (getter و setter) بده. به اینا POJO یا Java Bean هم میگن.

```java
public class Employee {
    // ۱. مخفی‌سازی داده
    private double salary;

    // ۲. دسترسی کنترل‌شده (Getter)
    public double getSalary() {
        // اینجا می‌تونی منطق بذاری. مثلاً فقط به HR اجازه دیدن بدی.
        return salary;
    }

    // ۳. تغییر کنترل‌شده (Setter)
    public void setSalary(double newSalary) {
        if (newSalary > 0) { // اعتبارسنجی
            this.salary = newSalary;
        }
    }
}
```

## 2.4 اصل دوم: وراثت (Inheritance)

**تعریف ساده:** ساختن کلاس جدید بر اساس کلاس موجود. کلاس جدید (زیرکلاس/فرزند) فیلدها و متدهای کلاس موجود (اَبَرکلاس/والد) را به ارث می‌بره. رابطه IS-A داریم: `Dog IS-A Animal`.

**مزیت اصلی:** استفاده مجدد از کد (Code Reusability).

---

# ❗️ سوال مصاحبه‌ای: تفاوت Association، Aggregation و Composition رو کامل توضیح بده

**جواب:** این سه تا رابطه، درجه‌های مختلف وابستگی بین دو کلاس رو نشون میدن. از ضعیف به قوی:

## ۱. Association (ارتباط) – ضعیف‌ترین

**تعریف:** وقتی دو کلاس با هم ارتباط دارن، اما هیچکدوم مالک دیگری نیست و هیچ وابستگی عمری (Lifecycle Dependency) بینشون نیست.

**تشبیه:** من و یه دکتر. من می‌رم پیش دکتر، ولی دکتر مال من نیست و من مال دکتر نیستم. هرکدوم می‌تونیم بدون دیگری وجود داشته باشیم.

**علامت:** Uses a / Is associated with

```java
// Association - دو کلاس کاملاً مستقل
class Doctor {
    public void treat(Patient patient) {
        // دکتر بیمار رو درمان می‌کنه
    }
}

class Patient {
    private String name;
    // بیمار می‌تونه بدون دکتر وجود داشته باشه
}

// استفاده
Doctor doc = new Doctor();
Patient p = new Patient();
doc.treat(p); // ارتباط، ولی مالکیت نیست
```

#### ویژگی‌ها:

- هر دو شیء مستقل هستن

- هیچکدوم مالک دیگری نیست

- عمر هرکدوم به دیگری وابسته نیست

- می‌تونه یک‌طرفه یا دوطرفه باشه


---

## ۲. Aggregation (تجمیع) – متوسط

**تعریف:** رابطه HAS-A ضعیف. یعنی یه کلاس شامل کلاس دیگه‌ست، ولی اون کلاس می‌تونه مستقل از مالکش هم وجود داشته باشه.

**تشبیه:** دانشگاه و دانشجوها. دانشگاه دانشجوها رو داره، ولی اگه دانشگاه تعطیل بشه، دانشجوها همچنان وجود دارن. (قبلاً گفتیم)

**علامت:** Has a (weak)

```java
// Aggregation - جزء می‌تونه مستقل وجود داشته باشه
class Student {
    private String name;
    // Student می‌تونه بدون University وجود داشته باشه
}

class University {
    private List<Student> students;
    
    public University(List<Student> students) {
        this.students = students; // از بیرون داده میشه
        // اگه University از بین بره، Studentها هنوز هستن
    }
}

// استفاده
Student s1 = new Student();
Student s2 = new Student();
List<Student> list = Arrays.asList(s1, s2);
University uni = new University(list); // دانشجوها قبل از دانشگاه بودن
```

#### ویژگی‌ها:

- شیء جزء می‌تونه بدون شیء کل وجود داشته باشه

- در Constructor از بیرون دریافت میشه (نه اینکه داخل ساخته بشه)

- رابطه "کل-جزء" (Whole-Part) هست


---

## ۳. Composition (ترکیب) – قوی‌ترین

**تعریف:** رابطه HAS-A قوی. یعنی جزء، بخشی از کل هست و بدون کل معنی نداره. اگه کل از بین بره، جزء هم از بین می‌ره.

**تشبیه:** خانه و اتاق‌ها. اتاق‌ها بدون خانه معنی ندارن. اگه خانه خراب بشه، اتاق‌ها هم از بین میرن.

**علامت:** Has a (strong) / Part of

```java
// Composition - جزء بدون کل وجود نداره
class Room {
    // Room بدون House معنی نداره
}

class House {
    private Room room; // Composition
    
    public House() {
        this.room = new Room(); // داخل خودش ساخته میشه
        // اگه House از بین بره، Room هم می‌میره
    }
}

// استفاده
House house = new House(); // با ساخت House، Room هم ساخته میشه
// وقتی house از بین بره، room هم از بین می‌ره
```

#### ویژگی‌ها:

- شیء جزء بدون شیء کل وجود نداره

- داخل Constructor خود کل ساخته میشه (نه از بیرون)

- با نابودی کل، جزء هم نابود میشه


---

## 📊 جدول مقایسه سه رابطه

| ویژگی | Association | Aggregation | Composition |
|-------|-------------|-------------|-------------|
| قدرت | ضعیف 🟢 | متوسط 🟡 | قوی 🔴 |
| وابستگی عمری | هیچی | کم | کامل |
| جزء مستقل؟ | کاملاً مستقل | مستقل | وابسته به کل |
| مثال | دکتر و بیمار | دانشگاه و دانشجو | خانه و اتاق |
| در کد | پارامتر متد | از بیرون دریافت | داخل ساخته میشه |
| نماد UML | خط ساده ◀─── | لوزی توخالی ◇─── | لوزی پر ◆─── |

## 🎯 خلاصه کاربردی برای مصاحبه

```text
Association (ارتباط)     → "Uses a"     → ضعیف‌ترین
Aggregation (تجمیع)      → "Has a"      → متوسط (جزء مستقل)
Composition (ترکیب)      → "Has a"      → قوی‌ترین (جزء وابسته)
```

### نکته طلایی برای مصاحبه:

> "همیشه سعی کن به جای Inheritance (IS-A) از Composition (HAS-A) استفاده کنی. این قانون طلایی طراحی شی‌گراست."

---

## 📝 مثال کامل در یک کلاس

```java
// ۱. Association - فقط استفاده
class Driver {
    public void drive(Car car) {  // Association
        car.move();
    }
}

// ۲. Aggregation - جمع شدن
class Player {
    private String name;
    // Player می‌تونه تنها هم باشه
}

class Team {
    private List<Player> players;  // Aggregation
    
    public Team(List<Player> players) {
        this.players = players;  // از بیرون میاد
    }
}

// ۳. Composition - بخشی از کل
class Engine {
    // Engine بدون Car معنی نداره
}

class Car {
    private Engine engine;  // Composition
    
    public Car() {
        this.engine = new Engine();  // داخل ساخته میشه
    }
}
```

---

### ❗️ سوال مصاحبه‌ای ۲: چرا "ترکیب (Composition) را بر وراثت ترجیح بده" یه اصل طراحی معروفه؟ مشکل وراثت چیه؟

**جواب حیاتی:** وراثت باعث ایجاد جفت‌شدگی شدید (Tight Coupling) بین والد و فرزند میشه. یعنی:

- تغییر در کلاس والد می‌تونه تمام کلاس‌های فرزند رو بشکنه. (بهش میگن مشکل Fragile Base Class).
- وراثت، جزئیات پیاده‌سازی والد رو برای فرزند فاش می‌کنه (نقض کپسوله‌سازی).
- در جاوا که فقط یه والد می‌تونی داشته باشی، خیلی محدودکننده است.

**ترکیب (Composition):** یعنی به جای `extends` کردن، یه نمونه از اون کلاس رو به عنوان یه فیلد در کلاس خودت داشته باشی (HAS-A). اینطوری انعطاف‌پذیری خیلی بالاتری داری و می‌تونی رفتار رو در زمان اجرا عوض کنی.

```java
// a. روش بد (وراثت):
class TaxCalculator {
    double calculate() { ... }
}
class IranTaxCalculator extends TaxCalculator { ... } // tight coupling

// b. روش خوب (ترکیب):
interface TaxStrategy { double calculate(); }
class IranTax implements TaxStrategy { ... }
class USATax implements TaxStrategy { ... }

class Invoice {
    private TaxStrategy tax; // Composition!

    public Invoice(TaxStrategy tax) {
        this.tax = tax;
    }
}
// c. حالا هر وقت خواستی، استراتژی مالیاتی رو عوض می‌کنی
new Invoice(new IranTax());
new Invoice(new USATax());
```

### ❗️ سوال مصاحبه‌ای ۲.۵: فرق Composition و Aggregation چیه؟

**جواب:** هر دو نوعی رابطه HAS-A هستن، ولی تفاوت در **طول عمر** هست:

- **Composition (ترکیب - قوی):** شیء فرزند بدون والد وجود نداره. اگر والد بمیره، فرزند هم می‌میره.
- **Aggregation (تجمع - ضعیف):** شیء فرزند می‌تونه مستقل از والد وجود داشته باشه.

```java
// Composition - خانه و اتاق‌ها
class Room { ... }
class House {
    private Room room; // با ساخت خانه، اتاق ساخته میشه
    public House() {
        this.room = new Room(); // اتاق فقط درون خانه معنی داره
    }
}

// Aggregation - دانشگاه و دانشجو
class Student { ... }
class University {
    private List<Student> students; // دانشجو قبل از دانشگاه وجود داشته
    public University(List<Student> students) {
        this.students = students; // دانشجو مستقل از دانشگاه هست
    }
}

## 2.5 اصل سوم: چندریختی (Polymorphism)

**تعریف ساده:** "چندشکلی". توانایی یه ارجاع (Reference) از نوع والد، برای اشاره به اشیاء از نوع فرزند. یا صدا زدن یه متد یکسان و گرفتن رفتارهای متفاوت بر اساس نوع شیء.

دو نوع داریم:

### الف) چندریختی زمان کامپایل (Compile-Time) یا Method Overloading

یه کلاس، چندین متد با نام یکسان ولی پارامترهای متفاوت (تعداد، نوع، یا ترتیب) داره. کامپایلر با توجه به آرگومان‌های ورودی، تشخیص میده کدوم متد صدا زده بشه.

```java
class Printer {
    void print(String s) { ... }
    void print(int i) { ... }
    void print(String s, int copies) { ... }
}
```

---

### ❗️ سوال مصاحبه‌ای ۳: آیا با تغییر نوع بازگشتی می‌تونیم Overload کنیم؟

**جواب:** نه! نوع بازگشتی امضای متد نیست و به تنهایی برای Overloading کافی نیست. کامپایلر موقع صدا زدن متد، فقط به اسم متد و پارامترها نگاه می‌کنه.

### ب) چندریختی زمان اجرا (Runtime) یا Method Overriding

کلاس فرزند، یه متد از کلاس والد رو با دقیقاً همون امضا (اسم، پارامترها، نوع بازگشتی) بازنویسی (`@Override`) می‌کنه. تصمیم‌گیری برای اینکه کدوم نسخه از متد اجرا بشه، در زمان اجرا و بر اساس نوع واقعی شیء گرفته میشه. به این میگن Dynamic Method Dispatch.

```java
class Animal {
    void sound() { System.out.println("Some sound"); }
}
class Dog extends Animal {
    @Override void sound() { System.out.println("Woof!"); }
}
class Cat extends Animal {
    @Override void sound() { System.out.println("Meow"); }
}

public class Main {
    public static void main(String[] args) {
        Animal myPet; // یه ارجاع از نوع Animal

        myPet = new Dog();
        myPet.sound(); // خروجی: Woof! (رفتار Dog)

        myPet = new Cat();
        myPet.sound(); // خروجی: Meow (رفتار Cat)
    }
}
```

---

### ❗️ سوال مصاحبه‌ای ۴: `@Override` annotation چیه و آیا اجباریه؟

**جواب:** اجباری نیست، ولی به شدت توصیه میشه. این Annotation به کامپایلر میگه "من قصد دارم یه متد از والد رو بازنویسی کنم". اگه اشتباهی اسم متد رو غلط بنویسی یا پارامترهاش رو اشتباه بگیری، کامپایلر بهت خطا میده و نمی‌ذاره. بدون `@Override`، متدت یه متد جدید محسوب میشه و چندریختی اتفاق نمی‌افته.

## 2.6 اصل چهارم: انتزاع (Abstraction)

**تعریف ساده:** پنهان کردن پیچیدگی و نمایش فقط اطلاعات ضروری. یعنی بگی چه کاری انجام میشه، نه چطور.

**چرا مهمه؟** پیچیدگی سیستم رو کم میکنه و به کاربر (یا کلاس دیگر) اجازه میده با یه رابط ساده کار کنه.

**چطور پیاده میشه؟**

- **کلاس انتزاعی (Abstract Class):** کلاسیه که نمی‌تونی ازش شیء بسازی (`new` کنی). فقط برای اینه که زیرکلاس‌ها ازش ارث‌بری کنن. می‌تونه متدهای `abstract` (بدون بدنه) و متدهای معمولی داشته باشه.
- **رابط (Interface):** یه "قرارداد" کامله. قبل از Java 8 فقط می‌تونست متدهای `abstract` و ثابت‌ها (`static final`) داشته باشه. حالا می‌تونه متدهای `default` و `static` هم داشته باشه.

---

### ❗️ سوال مصاحبه‌ای ۵ (حیاتی): تفاوت Abstract Class و Interface رو کامل و با مثال توضیح بده

**جواب:**

**کلاس انتزاعی (Abstract Class):**

- با `extends` استفاده میشه (فقط یکی).
- می‌تونه constructor داشته باشه (برای مقداردهی به فیلدهای مشترک والد).
- می‌تونه متدهای معمولی (با بدنه) داشته باشه.
- فیلدهاش می‌تونن `private`, `protected` و غیره باشن.
- برای وقتی خوبه که کلاس‌های مرتبط، یه پیاده‌سازی مشترک دارن. (مثلاً `HttpServlet` که کلی کد مشترک داره).

**رابط (Interface):**

- با `implements` استفاده میشه (چندین تا می‌تونی پیاده‌سازی کنی).
- constructor نداره.
- متدهاش ذاتاً `public abstract` بودن. متدهای `default` و `static` هم الان داریم.
- فیلدهاش فقط `public static final` هستن (یعنی ثابت).
- برای وقتی خوبه که می‌خوای یه "قابلیت" یا "رفتار" مشترک بین کلاس‌های کاملاً نامرتبط تعریف کنی (مثلاً `Serializable`, `Comparable`).

```java
// Interface = قرارداد "چه کاری"
interface Payable {
    void pay(int amount); // همه‌شون باید بلد باشن پرداخت کنن
}

// Abstract Class = پایه مشترک
abstract class Employee {
    protected String name;
    public Employee(String name) { this.name = name; }
    abstract double calculateSalary(); // هر کارمندی حقوقش فرق داره
    public void clockIn() { System.out.println(name + " clocked in."); } // کار مشترک
}

// حالا استفاده
class FullTimeEmployee extends Employee implements Payable {
    public FullTimeEmployee(String name) { super(name); }
    @Override double calculateSalary() { return 5000; }
    @Override public void pay(int amount) { System.out.println("Paid via bank"); }
}
```

## 2.7 مفاهیم تکمیلی و حیاتی OOP

### ❗️ سوال مصاحبه‌ای ۶: `static` چیه؟ متد `static` با متد غیر `static` چه فرقی داره؟

**جواب:** `static` یعنی متعلق به کلاس، نه به یه نمونه (شیء) خاص.

- **فیلد `static`:** یه متغیر که بین همه اشیاء اون کلاس مشترکه. تغییرش در یکی، در همه اعمال میشه. (مثل شمارنده تعداد اشیاء ساخته شده).
- **متد `static`:** می‌تونی بدون ساختن شیء صداش بزنی (`ClassName.method()`). به فیلدهای غیر `static` (فیلد شیء) دسترسی نداره، چون اصلاً شیئی وجود نداره که بخواد بهش دسترسی داشته باشه! `main` هم `static` هست چون JVM بدون ساختن هیچ شیئی باید بتونه اجراش کنه.

---

### ❗️ سوال مصاحبه‌ای ۷: `final` چیه و کجاها استفاده میشه؟

**جواب:**

- **متغیر `final`:** یه ثابت میسازه. بعد از مقداردهی اولیه، دیگه نمیشه تغییرش داد.
- **متد `final`:** نمی‌شه در زیرکلاس Override اش کرد.
- **کلاس `final`:** نمی‌شه ازش ارث‌بری کرد (مثل `String`).

---

### ❗️ سوال مصاحبه‌ای ۸: رابطه IS-A و HAS-A رو با مثال بگو

- **IS-A:** وراثت (`extends`). `Dog` IS-A `Animal`.
- **HAS-A:** ترکیب (Composition). `Car` HAS-A `Engine`. (ماشین یه موتور داره، نه اینکه ماشین یه نوع موتور هست). همیشه اولویت با HAS-A است.

### ❗️ سوال مصاحبه‌ای ۸.۵: چرا Multiple Inheritance در جاوا ممنوعه؟

**جواب:** به خاطر مشکل Diamond Problem (الماس).

```text
        A
       / \
      B   C
       \ /
        D
```

اگه B و C هر دو یه متد از A رو Override کنن، D باید کدوم رو استفاده کنه؟ جاوا با حذف Multiple Inheritance برای کلاس‌ها، این مشکل رو حل کرد. ولی با Interfaceها این مشکل وجود نداره چون متدهای Interface بدنه ندارن (قبلاً).

راه حل جاوا: یک کلاس می‌تونه چندین Interface پیاده‌سازی کنه.

```java
interface Flyable { void fly(); }
interface Swimmable { void swim(); }
class Duck implements Flyable, Swimmable {
    // می‌تونه هر دو رو پیاده‌سازی کنه بدون مشکل
}
```
